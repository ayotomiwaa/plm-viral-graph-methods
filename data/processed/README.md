# Processed Data

Cleaned metadata, aligned sequences, accession panels, and derived local inputs are not tracked here unless they are small public manifests.

Document required local files in `data/manifests/`.
