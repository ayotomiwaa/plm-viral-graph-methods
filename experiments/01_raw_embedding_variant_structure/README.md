# Raw Embedding Variant Structure

## Question

Do raw PLM embedding vectors show non-random structure with respect to SARS-CoV-2 variant or lineage labels before graph construction?

## Method Role

This is a representation-level sanity check. It should not be used as a substitute for graph-native validation, but it helps determine whether embedding space has detectable lineage or variant organization.

## Local Sources

Historical local workspace:

- `analysis/raw_embedding_variant_clustering.py`
- `analysis/raw_embedding_variant_clustering_20k*`
- `ref_embeddins/embeddings_esm2_650M_with_wuhan_ref/`

Those folders remain local and untracked.

## Tracked Artifacts

Track only small final summaries or selected figures after review.
