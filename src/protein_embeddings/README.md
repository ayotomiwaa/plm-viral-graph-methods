# Source Package

This directory is the future home for stable, importable method code.

At this restructuring stage, the reusable workflow entry points are kept under `scripts/` because many were copied from experiment folders and still behave as command-line scripts. Promote code here only when it has a stable API and lightweight tests.
