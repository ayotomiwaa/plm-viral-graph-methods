# Tests

Add lightweight tests here as stable method code is promoted into `src/`.

Prefer small synthetic fixtures over real sequence, embedding, or graph artifacts.

Current tests include synthetic checks for the paired k-medoids validation runner.
